import {styled} from "@mui/material/styles";
import {Box} from "@mui/material";

export const StyledSumCalculationContainer = styled(Box)({
    padding: '10px 0',
    '.sum-calculation-description': {
        marginTop: 10,
        marginBottom: 10,
    }
});